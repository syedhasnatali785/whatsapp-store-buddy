const CheckoutDialog = ({ cartItems }) => {  const generateWhatsAppMessage = () => {    const message = cartItems.map(item => {      return `Product: ${item.name}\nPrice: ${item.price}\nImage: ${item.image_url}`;    }).join('\n');    return message;  };  return (    /* Your component return JSX goes here */  );};